"""
Author: Jose Stovall | oitsjustjose
"""

SERVER_ENVS = {
    "allow_flight": bool,
    "allow_nether": bool,
    "announce_player_achievements": bool,
    "autopause_knock_interface": str,
    "autopause_period": int,
    "autopause_timeout_est": int,
    "autopause_timeout_init": int,
    "autopause_timeout_kn": int,
    "broadcast_console_to_ops": bool,
    "broadcast_rcon_to_ops": bool,
    "build_from_source": bool,
    "bukkit_download_url": str,
    "canyon_build": int,
    "cf_base_dir": str,
    "cf_server_mod": str,
    "cfg_db_host": str,
    "cfg_db_name": str,
    "cfg_db_password_file": str,
    "console": bool,
    "copy_config_dest": str,
    "copy_mods_dest": str,
    "custom_server": str,
    "difficulty": str,
    "disable_healthcheck": bool,
    "enable_autopause": bool,
    "enable_command_block": bool,
    "enable_jmx": bool,
    "enable_query": bool,
    "enable_rcon": bool,
    "enable_rolling_logs": bool,
    "enable_status": bool,
    "enforce_whitelist": bool,
    "entity_broadcast_range_percentage": float,
    "env_variable_prefix": str,
    "exec_directly": bool,
    "fabric_installer": str,
    "fabric_installer_url": str,
    "fabric_installer_version": str,
    "force_gamemode": bool,
    "force_redownload": bool,
    "force_world_copy": bool,
    "forge_installer": str,
    "forge_installer_url": str,
    "forgeversion": str,
    "ftb_legacyjavafixer": bool,
    "ftb_modpack_id": str,
    "ftb_modpack_version_id": str,
    "function_permission_level": int,
    "generate_structures": bool,
    "hardcore": bool,
    "icon": str,
    "init_memory": str,
    "jmx_host": str,
    "jvm_dd_opts": str,
    "jvm_opts": str,
    "jvm_xx_opts": str,
    "level": str,
    "level_type": str,
    "max_build_height": int,
    "max_memory": str,
    "max_players": int,
    "max_tick_time": int,
    "max_world_size": int,
    "memory": str,
    "modpack": str,
    "mohist_build": int,
    "motd": str,
    "network_compression_threshold": int,
    "online_mode": bool,
    "op_permission_level": int,
    "ops": str,
    "override_icon": bool,
    "override_ops": bool,
    "override_server_properties": bool,
    "override_whitelist": bool,
    "paper_download_url": str,
    "paperbuild": int,
    "player_idle_timeout": int,
    "plugins_sync_update": bool,
    "prevent_proxy_connections": bool,
    "proxy": str,
    "purpur_build": str,
    "pvp": bool,
    "query_port": str,
    "rcon_passord": str,
    "rcon_port": str,
    "remove_old_mods": bool,
    "remove_old_mods_depth": int,
    "remove_old_mods_exclude": str,
    "remove_old_mods_include": str,
    "replace_env_variables": bool,
    "replace_env_variables_exclude_paths": bool,
    "replace_env_variables_excludes": bool,
    "resource_pack": str,
    "resource_pack_sha1": str,
    "seed": str,
    "server_name": str,
    "server_port": str,
    "snooper_enabled": bool,
    "spawn_animals": bool,
    "spawn_monsters": bool,
    "spawn_npcs": bool,
    "spawn_protection": int,
    "spiget_resources": str,
    "spigot_download_url": str,
    "spongeversion": str,
    "stop_duration": int,
    "sync_chunk_writes": bool,
    "tune_nursery_sizes": bool,
    "tune_virtualized": bool,
    "type": str,
    "tz": str,
    "use_aikar_flags": bool,
    "use_flare_flags": bool,
    "use_large_pages": bool,
    "use_modpack_start_script": bool,
    "use_native_transport": bool,
    "view_distance": int,
    "whitelist": str,
    "world": str,
    "world_index": int,
}
